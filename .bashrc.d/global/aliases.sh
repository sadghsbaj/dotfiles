# System & Terminal
alias aliases='grep --color=auto "^alias" ~/.bashrc.d/global/aliases.sh'
alias a='grep --color=auto "^alias" ~/.bashrc.d/global/aliases.sh'
alias r='source ~/.bashrc'
alias c='clear'
alias dotfiles='cd ~/dotfiles'

# Git & Projects
alias add='git add .'
alias commit='git commit -m'

# SSH connections
# ssh pi5  (Raspberry Pi 5)
# ssh mama (Mom's Linux Laptop)
